# ── Frontend Environment Variables ──────────────────────────────────────────
# In local development (npm run dev), leave this empty or unset to use Vite's proxy (http://localhost:8080).
# In production (Vercel / Cloud), set this to your backend's public URL:
# Example: https://nous-backend.onrender.com
VITE_API_BASE_URL=
